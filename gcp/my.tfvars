# Refer to token secrets in environment variables
# Use the credentials file and specified shell session to set
# The token is ${access-key}:${secret-key}
rancher-token = "$${akey}:$${skey}"
rancher-url   = "$${rancher_url}"
ctlnumnodes = 1
numnodes = 4 # workers
delaysec = 180
